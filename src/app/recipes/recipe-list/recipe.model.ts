export class Recipe {
    public name: string;
    public description: string;
    public imagaPath: string;

    constructor(name: string, desc: string, imagePath: string) {
        this.name = name;
        this.description = desc;
        this.imagaPath = this.imagaPath;
    }
}
